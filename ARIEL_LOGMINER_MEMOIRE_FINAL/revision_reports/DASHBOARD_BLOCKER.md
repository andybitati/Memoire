# Blocage dashboard bleu

Le code source du dashboard bleu est identifié dans `web/dashboard/src/styles.css` et `web/dashboard/src/main.jsx`. Les quatre captures actuellement intégrées au dossier final (`captures/dashboard_*.png`) sont visuellement rouge-or et ne peuvent donc pas être utilisées comme preuve de l'interface bleue demandée.

La tentative de lancement/capture automatisée n'a pas abouti dans l'environnement courant : le serveur local démarre, mais Playwright n'est pas disponible hors cache et l'accès au registre npm est bloqué. Aucune capture rouge-or n'est déclarée comme dashboard bleu et aucune recolorisation artificielle n'est effectuée.

Statut : **BLOQUÉ — livraison non prête pour ZIP manuel** tant qu'une capture réelle du dashboard bleu n'a pas été produite et vérifiée visuellement.
