# Dépendances principales du projet TFE / Logminer.
#
# Installation:
#   python -m pip install -r requirements.txt

# Extraction de texte depuis les PDF du mémoire et de la documentation.
PyPDF2>=3.0.0

# Barres de progression optionnelles utilisées par certains parseurs.
tqdm>=4.66.0

# Lecture directe des fichiers Windows Event Log natifs (.evtx).
# Si l'installation échoue, exporter les journaux Windows en XML reste possible.
python-evtx>=0.7.4

# Dépendances utiles pour les futures étapes IA mentionnées dans le mémoire.
# Elles serviront aux notebooks/prototypes de détection d'anomalies.
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
joblib>=1.3.0

# Les backends deep learning du prototype LSTM sont separes dans
# requirements-ai.txt pour ne pas alourdir l'installation de base.

# Dashboard léger prévu pour la visualisation des alertes/anomalies.
streamlit>=1.36.0

# API REST V2 pour exposer les agents sans casser la V1 CLI.
fastapi>=0.139.0
starlette>=1.3.1
uvicorn[standard]>=0.30.0
httpx>=0.27.0
redis>=5.0.0
paho-mqtt>=2.1.0
python-multipart>=0.0.20

# Mesure de consommation CPU, memoire, disque et processus pour le dashboard.
psutil>=5.9.0

# Exports des figures SVG du memoire en PDF/PNG pour articles.
svglib>=1.6.0
reportlab>=4.5.0
